import { ProgramHeadNotifications } from '@/components/program-head/program-head-notifications';

export const metadata = {
  title: 'Notifications - Department Chair'
};

export default function Page() {
  return <ProgramHeadNotifications />;
}
