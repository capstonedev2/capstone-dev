import { ProgramHeadReports } from '@/components/program-head/program-head-reports';

export const metadata = {
  title: 'Reports & Analytics - Program Head'
};

export default function Page() {
  return <ProgramHeadReports />;
}
