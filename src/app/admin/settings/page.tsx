import { AdminSettings } from '@/components/admin/admin-settings';

export const metadata = {
  title: 'Roles and Access - Research Head'
};

export default function Page() {
  return <AdminSettings />;
}
