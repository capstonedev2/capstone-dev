import { LibrarySaved } from '@/components/library/library-saved';

export const metadata = {
  title: 'Saved Projects - E-Library Portal'
};

export default function Page() {
  return <LibrarySaved />;
}
