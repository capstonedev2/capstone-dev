import { LibraryRepository } from '@/components/library/library-repository';

export const metadata = {
  title: 'Repository - E-Library Portal'
};

export default function Page() {
  return <LibraryRepository />;
}
