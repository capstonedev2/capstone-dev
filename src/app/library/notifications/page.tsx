import { LibraryNotifications } from '@/components/library/library-notifications';

export const metadata = {
  title: 'Notifications - E-Library Portal'
};

export default function Page() {
  return <LibraryNotifications />;
}
