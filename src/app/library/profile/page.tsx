import { LibraryProfile } from '@/components/library/library-profile';

export const metadata = {
  title: 'Profile - E-Library Portal'
};

export default function Page() {
  return <LibraryProfile />;
}
