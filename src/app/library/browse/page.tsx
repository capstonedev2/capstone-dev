import { LibraryBrowse } from '@/components/library/library-browse';

export const metadata = {
  title: 'Browse Projects - E-Library Portal'
};

export default function Page() {
  return <LibraryBrowse />;
}
