import { PartnerFeedback } from '@/components/partner/partner-feedback';

export const metadata = {
  title: 'Feedback & Reports - Partner / Beneficiary'
};

export default function Page() {
  return <PartnerFeedback />;
}
