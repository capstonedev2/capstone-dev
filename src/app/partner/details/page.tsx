import { PartnerDetails } from '@/components/partner/partner-details';

export const metadata = {
  title: 'Project Details - Partner / Beneficiary'
};

export default function Page() {
  return <PartnerDetails />;
}
