import { PartnerImplementations } from '@/components/partner/partner-implementations';

export const metadata = {
  title: 'Active Implementations - Partner / Beneficiary'
};

export default function Page() {
  return <PartnerImplementations />;
}
