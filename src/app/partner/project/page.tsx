import { PartnerProject } from '@/components/partner/partner-project';

export const metadata = {
  title: 'Browse Projects - Partner / Beneficiary'
};

export default function Page() {
  return <PartnerProject />;
}
