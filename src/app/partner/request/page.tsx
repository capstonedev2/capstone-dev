import { PartnerRequest } from '@/components/partner/partner-request';

export const metadata = {
  title: 'Request Adoption - Partner / Beneficiary'
};

export default function Page() {
  return <PartnerRequest />;
}
