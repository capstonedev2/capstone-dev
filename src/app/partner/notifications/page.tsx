import { PartnerNotifications } from '@/components/partner/partner-notifications';

export const metadata = {
  title: 'Notifications - Partner / Beneficiary'
};

export default function Page() {
  return <PartnerNotifications />;
}
