import { PartnerDashboard } from '@/components/partner/partner-dashboard';

export const metadata = {
  title: 'Partner / Beneficiary - ThesisTrack'
};

export default function Page() {
  return <PartnerDashboard />;
}
